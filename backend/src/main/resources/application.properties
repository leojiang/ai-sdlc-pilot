spring.application.name=aisdlc-backend

# Health endpoint is the wiring contract for the frontend smoke screen (story #1 AC3).
# Keep 200 + {"status":"UP"} stable.
management.endpoints.web.exposure.include=health

# Port override documented in README: SERVER_PORT=8081 ./mvnw spring-boot:run
